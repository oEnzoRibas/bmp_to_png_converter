# Specify the minimum required CMake version
cmake_minimum_required(VERSION 3.10)

# Set the project name
project(bmp_to_png_converter)

# Add an executable target
add_executable(bmptopngconverter src/main.c)